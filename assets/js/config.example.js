/*
 * Committed placeholder — shows the shape config.js will take.
 * The real assets/js/config.js is generated by scripts/generate-config.js
 * from .env.local and is gitignored; it is never committed.
 */
window.WANGOES_CONFIG = {
  SCRIPT_URL: '', // populated from NEXT_PUBLIC_SCRIPT_URL at deploy time
  N8N_CTA_WEBHOOK_URL: '' // populated from NEXT_PUBLIC_N8N_CTA_WEBHOOK_URL at deploy time
};
